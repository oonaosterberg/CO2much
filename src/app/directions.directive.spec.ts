import { DirectionsDirective } from './directions.directive';

describe('DirectionsDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectionsDirective();
    expect(directive).toBeTruthy();
  });
});
